#include <iostream>
#include <cmath>
using namespace std;

int main(){
	double n, Odd, Even, increment;
	cout<<"Enter the list of number: "<<endl;
	cin>>n;
	if {
	Even = (n%2 == 0);
	Odd = (n%1 ==0);
}
	else{
	increment odd counter;
	}
	return 0;
}

